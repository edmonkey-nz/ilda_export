// c4d_symbols.h -- minimal resource header for the ILDA export plugin.
// This file only needs to exist and parse; the saver defines no dialog or
// description symbols, so a single placeholder keeps the enum non-empty.

enum
{
  ILDA_EXPORT_DUMMY = 10000,

  // End of symbol definition
};
